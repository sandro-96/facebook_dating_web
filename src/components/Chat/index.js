import "./index.scss"
import {useContext, useEffect, useState} from "react";

export const Chat = () => {
    useEffect(() => {
    }, []);

    return (
        <div className="chat-wrap">
            <div>Profile</div>
        </div>
    )
}
Chat.propTypes = {};

export default Chat;